{
  "name": "react-currency-masked-input",
  "version": "0.0.2",
  "description": "A react component that masks an input for currency.",
  "main": "dist/react-currency-masked-input.js",
  "devDependencies": {
    "babel": "^5.4.7"
  },
  "scripts": {
    "compile": "./node_modules/.bin/babel src/ --out-dir dist/"
  }
}
